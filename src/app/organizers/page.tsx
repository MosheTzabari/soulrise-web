import type { Metadata } from "next";
import Link from "next/link";
import { OrganizerForm } from "@/components/organizer-form";
import { PageShell } from "@/components/shared/page-shell";
export const metadata: Metadata = { title: "For organizers", description: "Apply for SoulRise organizer access and learn about the review process.", alternates: { canonical: "/organizers" } };
export default function Page() { return <PageShell path="/organizers">{({ copy, locale }) => <><section className="inner-hero"><div className="shell narrow"><p className="eyebrow">SoulRise · {copy.footer.organizers}</p><h1>{copy.organizers.title}</h1><p className="lead">{copy.organizers.lead}</p><Link href="/" className="text-link"><span aria-hidden="true">{locale === "he" ? "→" : "←"}</span> {copy.common.home}</Link></div></section><section className="section"><div className="shell split"><div><h2>{copy.organizers.processTitle}</h2><ul className="check-list">{copy.organizers.capabilities.map(x => <li key={x}>{x}</li>)}</ul><p className="notice">{copy.organizers.note}</p></div><div className="process-list">{copy.organizers.process.map((x, i) => <article key={x.title}><span>0{i + 1}</span><div><h3>{x.title}</h3><p>{x.body}</p></div></article>)}</div></div></section><section className="section surface-section"><div className="shell form-layout"><div className="section-heading"><p className="eyebrow">{copy.common.application}</p><h2>{copy.organizers.formTitle}</h2><p>{copy.organizers.formIntro}</p></div><OrganizerForm copy={copy.organizers} /></div></section></>}</PageShell>; }
