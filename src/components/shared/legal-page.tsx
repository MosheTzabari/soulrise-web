import { PageShell } from "./page-shell";
// NOT PRODUCTION-FINAL: approved legal counsel copy must replace this content before launch.
export function LegalPage({ kind }: { kind: "privacy" | "terms" }) { const path = kind === "privacy" ? "/privacy" : "/terms"; return <PageShell path={path}>{({ copy }) => <><section className="inner-hero"><div className="shell legal-copy"><p className="eyebrow">Legal · {copy.common.temporary}</p><h1>{kind === "privacy" ? copy.legal.privacyTitle : copy.legal.termsTitle}</h1><div className="legal-warning" role="note">{copy.legal.notice}</div><p className="lead">{copy.legal.intro}</p></div></section><section className="section"><div className="shell legal-copy">{copy.legal.sections.map(x => <section key={x.title}><h2>{x.title}</h2><p>{x.body}</p></section>)}</div></section></>}</PageShell>; }
